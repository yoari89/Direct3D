#pragma once
#include "Base/PreProcessor.h"

ENGINE_BEGIN

enum AXIS
{
	AXIS_X,
	AXIS_Y,
	AXIS_Z,
	AXIS_END
};

enum COMPONENT_TYPE
{
	CT_TRANSFORM,
	CT_RENDERER,
	CT_CAMERA,
	CT_MATERIAL,
	CT_ANIMATION2D,
	CT_COLLIDER,
	CT_UI,
	CT_TEXT,
	CT_SOUND,
	CT_STAGE2D,
	CT_TILE2D,
	CT_END
};

enum CAMERA_TYPE
{
	CT_PERSPECTIVE,
	CT_ORTHO
};

enum SHADER_TYPE
{
	ST_VERTEX,
	ST_PIXEL,
	ST_END
};

enum CBUFFER_SHADER_TYPE
{
	CST_VERTEX	= 0X1,
	CST_PIXEL	= 0X2
};

enum RENDER_STATE
{
	RS_BLEND,
	RS_RASTERIZER,
	RS_DEPTHSTENCIL,
	RS_END
};

enum GAME_MODE
{
	GM_2D,
	GM_3D
};

enum ANIMATION2D_TYPE
{
	A2D_ATLAS,
	A2D_FRAME
};

enum ANIMATION_OPTION
{
	AO_LOOP,
	AO_DISABLE,
	AO_ONCE_DESTROY,
	AO_ONCE_RETURN
};

enum COLLIDER_TYPE
{
	CT_POINT,
	CT_RECT,
	CT_CIRCLE,
	CT_PIXEL,
	CT_OBB,
	CT_MAX
};

enum eUserColType
{
	UCT_NONE,
	UCT_AREA,
	UCT_WALL,
	UCT_PLAYER,
	UCT_PLAYER_SKILL,
	UCT_MONSETR,
	UCT_HAT,
	UCT_MONSTER_ATTACK,
	UCT_SUSTAINED_ATTACK,
	UCT_MONSTER_DEATH_ATTACK,
	UCT_MAX
};

enum COLLISION_CALLBACK_TYPE
{
	CCT_ENTER,
	CCT_STAY,
	CCT_LEAVE,
	CCT_END
};

enum COLLISION_GROUP_TYPE
{
	CGT_2D,
	CGT_3D
};

enum UI_TYPE
{
	UT_PANEL,
	UT_BUTTON,
	UT_CHECKBOX,
	UT_BAR
};

enum BUTTON_STATE
{
	BS_NORMAL,
	BS_MOUSEON,
	BS_CLICK,
	BS_DISABLE,
	BS_END
};

enum BAR_DIR
{
	BD_LEFT,
	BD_RIGHT,
	BD_UP,
	BD_DOWN
};

enum TRANSFORM_PARENT_FLAG
{
	TPF_SCALE = 0x1,
	TPF_ROT = 0x2,
	TPF_POS = 0x4
};

enum STAGE2D_TILE_TYPE
{
	S2D_NONE,
	S2D_RECT,
	S2D_ISOMETRIC
};

// === Fade ===

enum eFadeType
{
	FADETYPE_IN,
	FADETYPE_OUT,
};

enum eFadeInState
{
	FIS_READY,
	FIS_START,
	FIS_END
};

enum eFadeOutState
{
	FOS_READY,
	FOS_START,
	FOS_END
};

// === Sound ===

enum eSoundType
{
	SOUNDTYPE_BGM,
	SOUNDTYPE_EFFECT,
	SOUNDTYPE_MAX
};

//enum eLoadType
//{
//	LOAD_TYPE_NONE,
//	LOAD_TYPE_TEXTURE,
//	LOAD_TYPE_SOUND,
//	LOAD_TYPE_MAX
//};

ENGINE_END